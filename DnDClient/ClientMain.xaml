<Window x:Class="DnDClient.ClientMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dungeons &amp; Dragons" Height="350" Width="525" Closed="Window_Closed" Icon="icon.ico">
    <Grid>
        <Button x:Name="submitButton" Content="Wyślij" Margin="0,0,155,10" Click="Button_Click" Height="23" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="75"/>
        <ListBox x:Name="usersListBox" HorizontalAlignment="Right" Margin="0,10,10,10" Width="140"/>
        <ListBox x:Name="chatListBox" Margin="10,10,155,38"/>
        <TextBox x:Name="chatTextBox" Margin="10,0,235,10" TextWrapping="Wrap" Height="23" VerticalAlignment="Bottom" KeyDown="chatTextBox_KeyDown"/>

    </Grid>
</Window>
